language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t paulichdom/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true paulichdom/docker-react npm run test -- --coverage
